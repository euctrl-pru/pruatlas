# Return the polygon represent the spherical Earth.

Return the polygon represent the spherical Earth.

## Usage

``` r
sphere(crs = pruatlas::pru_laea_proj)
```

## Arguments

- crs:

  a `proj` projection string

## Value

A Simple Feature representing the spherical contour of the Earch in the
relevant projection.
